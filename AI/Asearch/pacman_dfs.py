pac_pos = setPos(raw_input());
food_pos = setPos(raw_input());
grid_bounds = setPos(raw_input());

def setPos(str):
    arr = map(int, str.split(" "))
    p = {}
    p.y = arr[0]
    p.x = arr[1]
    return p

for 
